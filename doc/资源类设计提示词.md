我现在有三种数据，需要建立特定的数据结构来储存。
1.每个group的简略信息
2.一个group下所有resource的简略信息
3.一个resource的详细信息
我想要的结构如下：
1.最底层的类是Resource类，直接使用request_resource这个请求方法示例中的信息进行初始化，然后设置若干get方法供外部访问
2.然后是ResourceFolder类，维护两个列表，一个用来储存下一级的ResourceFolder，一个用来储存下一级的Resource，设置一些方法，比如根据resource的path_id(也就是id，即资源路径中最后的那一个)来递归地搜索并返回一个资源。还有根据path_id递归地搜索一个ResourceFolder
3.然后是一个ResourceTree类，根据request_groupz方法中的示例数据来初始化一个资源树。并对外提供一些方法。注意，这个示例数据的列表中包含大量字典，每个字典里都有一个path，表示该资源/资源文件夹在整个目录中的结构
4.然后再建立一个Group类，用来储存一个课程的所有信息，其中最关键的信息就是这个课程的ResourceTree，然后还有一些其它的方法，比如获取课程的介绍，group_id等，这些信息在request_all_groupsd的示例数据中
5.最后，建立一个GroupManager类，储存所有Group类。
初始化的顺序如下：
1.用户使用login_manager类初始化GroupManager，程序使用login_manager中的session和headers来请求request_all_groups的数据来完成初始化
2.GroupManager中负责初始化所有的group，它可以根据上一级传来的数据找到许多group_id，结合传来的session即可请求request_groupd的数据，使用这些数据就可以初始化若干个resourceTree类
3.在建立resource类的过程中，每个资源/资源文件夹的具体数据，则通过请求request_resource来获得。
请你按照这些要求为我建立这些类，由于需要建立的类较多，请你一个个慢慢建立。如果一个类过长，可以迭代输出，避免一次性输出太多代码导致卡死。
